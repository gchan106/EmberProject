<div>
    <Navbar
        @changePage={{this.navigateTo}}
        @betInfoHandler={{this.betInfoHandler}}
    />
</div>


{{#if this.isProfilePage}}
    <div class = 'left'>
    <ProfilePage 
        @model={{@model}}
        @changePage={{this.navigateTo}}
        @betInfoHandler={{this.betInfoHandler}}
    />
    </div>

    <div class = 'right'>
    <FriendsList
    @model={{@model}}
    />
    {{!-- <AddFriend
    @model={{@model}}
    /> --}}
    </div>
    
    
{{/if}}

{{#if this.isCreateBetPage}}
    <div class = 'left'>
    <IndividualBet 
        @model={{@model}}
        @betId={{this.betID}}
        @displayCreateBet={{this.displayCreateBet}}
        @isAdmin={{this.isAdmin}}
    />
    </div>

    <div class = 'right'>
    <FriendsList
     @model={{@model}}
    />
    {{!-- <AddFriend
     @model={{@model}}
    /> --}}
    </div>
    
{{/if}}

{{#if this.isActiveBetPage}}
    <div class = 'left'>
    <ActiveBets 
        @model={{@model}}
        @betId={{this.betID}}
        @changePage={{this.navigateTo}}
        @betJoin={{this.betJoin}}

    />
    </div>

    <div class = 'right'>
    <FriendsList
    @model={{@model}}
    />
    
    {{!-- <AddFriend
    @model={{@model}}
    /> --}}
    </div>
    
{{/if}}

{{#if this.isSignUpPage}}
    <SignUp
        @model={{@model}}
    />
{{/if}}